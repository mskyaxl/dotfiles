- defaults:
    link:
        relink: true
        create: true
        force: true
    shell:
        stdout: true
        stderr: true
        quiet: true

- create:
  - ~/.config

- clean:
    ~/:
       force: true
    ~/.config:
       recursive: true
