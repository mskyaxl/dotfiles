# Configure defaults
- defaults:
    include:
        isolated: True

# When only provided a string, that will be treated as the path of the file to include
- include: 'meta/configs/clangd.yaml'
- include: 'meta/configs/rust.yaml'
- include: 'meta/configs/androiddev.yaml'
- include: 'meta/configs/arduinodev.yaml'

