# Configure defaults
- defaults:
    include:
     isolated: True

# When only provided a string, that will be treated as the path of the file to include
- include: 'meta/profiles/base.yaml'
- include: 'meta/configs/alacritty.yaml'

